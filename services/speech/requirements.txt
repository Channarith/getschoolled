fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0
pytest==8.2.2
# Free neural TTS (Microsoft Edge voices). Without this the speech image has NO
# neural engine, so /tts returns 501 and clients fall back to the robotic
# on-device voice. edge-tts lives in aoep-shared's [presenter] extra, which the
# Dockerfile does NOT install, so pin it here. (Needs egress to the edge-tts
# endpoint at runtime; set ELEVENLABS_API_KEY for the most natural voice.)
edge-tts==6.1.12
# Installed editable in dev: ../../packages/shared (eduplatform-shared)
