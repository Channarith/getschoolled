# Pinned dependencies for the webcam-recognition lab (self-contained sub-project).
# Install with:  python3 -m pip install -r requirements.txt
# For real server-side silhouette detection also install the [vision] extra deps
# (opencv + numpy) listed below.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.32

# --- vision extra (real, CPU-only, self-hosted silhouette + face detection) ---
opencv-contrib-python-headless==4.10.0.84
numpy==2.2.6

# --- test extra ---
pytest==8.2.2
httpx==0.27.0
