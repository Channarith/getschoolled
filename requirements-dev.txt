# Consolidated backend dev dependencies for the whole monorepo (single stack).
# Installs the shared package editable, plus everything the Python services need
# to run and be tested locally. Heavy GPU/network packages (livekit-agents,
# vLLM, Whisper/XTTS) stay in per-service extras; the face-recognition CPU deps
# are included so the perception suite runs after a fresh setup.
-e packages/shared

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.27.0
python-multipart==0.0.32
pytest==8.2.2
# QA: coverage + lint (used by `make coverage` / `make lint` / qa/regression.py).
pytest-cov==5.0.0
ruff==0.6.9
PyYAML==6.0.2

# Perception (face recognition) - CPU only; model weights downloaded at runtime.
opencv-contrib-python-headless==4.10.0.84
onnxruntime==1.18.1
numpy==1.26.4

# Content scraper (curriculum ingestion: textbooks / web / video transcripts).
pypdf==4.2.0
beautifulsoup4==4.12.3
requests==2.32.3
fpdf2==2.7.9  # test-only: generates PDF fixtures for the scraper tests

# Harvester multi-source extractors (PowerPoint / Word). Imported lazily; only
# needed when generating a course from .pptx / .docx input.
python-pptx==0.6.23
python-docx==1.1.2
