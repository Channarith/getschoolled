CHANGELOG - Agentic Online Education Platform
=============================================
Format: newest first. No markdown per project convention.

[unreleased]
- 2026-08-18 - v0.46.81 - Harden shared config with env_int/env_float helpers, move assessment signing key into aoep_shared.auth; fix cache-rule segment matching, no-store on authenticated responses, knowledge-store DB recovery, copy-on-return for scenario/technique/word-map caches, SDK loopback URL parsing, jobs result cache bound.
- 2026-07-24 - v0.45.6 - Remove duplicate Salareen Worlds banner on Arcade page.
- 2026-07-24 - v0.45.5 - Skip onboarding step 1 (Your info) after home page signup: auto-submit profile with name from email prefix then jump to step 1 (Choose Plan) so users go directly to plan selection. Also fix: Redis pod was Pending for 2+ days due to PVC requesting 10GB (Vultr minimum is 40GB) causing all identity pods to run in-memory-only mode and cross-pod account-not-found 401s; fixed PVC to 40Gi and restored Redis connectivity.
- 2026-07-24 - v0.45.4 - Fix Apple Sign In: com.aiclassroom.web is the correct Services ID (exists and Sign In with Apple enabled); home page redirectURI updated to /login to match registered Apple return URL; revert incorrect com.aiclassroom.app change.
- 2026-07-24 - v0.45.3 - Apple Sign In: change APPLE_SERVICES_ID from com.aiclassroom.web to com.aiclassroom.app in web code and k8s secret.
- 2026-07-24 - v0.45.2 - Critical: identity service cross-pod account sync — when a valid token references an account not in the pod memory (created by a different replica), reload from Redis before returning 401. Fixes signup on pod A -> auth/me on pod B returning account not found.
- 2026-07-24 - v0.45.1 - Onboarding: AUTH_EVENT listener rescues session mid-flow; clickable step pills (navigate back to visited steps); Back button on steps 1-3; maxStep tracking prevents skipping ahead.
- 2026-07-24 - v0.44.2 - Fix auth 401 cascade (token cleared on first 401 from transient error, locking user out of onboarding — now requires 2 consecutive 401s); fix forgot password link on home page (/login?mode=forgot → /forgot-password).
- 2026-07-23 - v0.44.1 - Fix billing page 404 (ingress routed /billing to billing service instead of web app; add Exact /billing → web before the prefix catch-all); fix social login invisible on signup page (Google/Facebook/Apple buttons were gated on mode==login, now show on both modes); add inline password validation feedback on signup; 36 comprehensive onboarding tests covering email/password signup, OAuth social login, forgot/reset password, onboarding profile/plan, and full end-to-end journeys.
- 2026-07-23 - v0.42.1 - Fix 18 confirmed audit-v5 bugs: voucher consume endpoint + orchestrator integration; student-as-instructor (is_student_session field); confirm_group_class_payment auth enforced; submit button file-only unblocked; TOTP replay Redis-backed; WebGL context loss handler; handleInteract() extracted for mobile touch; touch tap one-frame-late fixed; screen share + camera unmount cleanup; confirmationTimerRef shared timer separated; start_group_class idempotency; VoucherStore._load() error logging; dead pageRef removed; Zelle creates PAYMENT_PENDING; changelog rolled over into released 0.42.0.
- 2026-07-23 - v0.43.0 - Fix 18 confirmed audit-v5 bugs: voucher consume endpoint added + called in orchestrator free-pass path; paid-path stores voucher_code for deferred consume; student-as-instructor fixed (is_student_session field, backend skips instructor_account_id); confirm_group_class_payment now requires auth; WebGL context loss handler; mobile Interact button extracted to handleInteract() callable from touch; touch tap one-frame-late fixed (merge before clear); screen share + camera tracks stopped on live-room unmount; confirmationTimerRef shared timer fixed (separate narrationContextTimerRef); start_group_class idempotency guard; TOTP replay Redis-backed; VoucherStore._load() no longer silently discards corrupt data; dead pageRef removed from PresenceHeartbeat; Zelle button creates PAYMENT_PENDING record; submit button unblocked for file-only upload.
- 2026-07-23 - v0.42.0 - Host a class: presentation file is now optional (host can use any catalog lesson and control slides manually without AI narration); button no longer requires a file; error message improved when PDF/PPTX library missing in pod; orchestrator requirements.txt adds pypdf+python-pptx so next Docker build will include them.
- 2026-07-23 - v0.40.0 - Fix 8 confirmed bugs from code review: (1) CRITICAL: _time.time() NameError crashing all presence pings with 500 (was import time, used as _time); (2) pypdf/python-pptx missing from orchestrator requirements causing presentation upload 422; (3) virtual Jump/Interact buttons non-functional on mobile (touch writes keysRef but jump/interact read keyJustDown — fixed with touchJustTappedRef bridge); (4) student join form created instructor sessions (now has separate onScheduleAsStudent handler); (5) Google email_verified string bypass (str "false" is truthy — now checks not in (True,"true",1)); (6) injectedJavaScript fires after page load (switched to injectedJavaScriptBeforeContentLoaded so __SALAREEN_NATIVE__ is set before game scripts run); (7) Quit button inverted when confirm silently suppressed (restructured to default-to-quit on suppression); (8) CorporateScreen passed "corporate" as free-text q not category filter.
- 2026-07-23 - v0.39.0 - Fix Worlds mobile crash: add ErrorBoundary with friendly reload/back UI, guard confirm() and requestFullscreen() calls that throw in iOS WebView, guard setAiXp/setAiMessage behind alive check to prevent React updates after unmount. Fix bug reporter 500: _append_index now creates parent directory before writing; endpoint catches OSError and returns 500 detail instead of unhandled crash.
- 2026-07-23 - v0.38.0 - Group Classes: remove Host a Class banner (redundant with tabs); Join a Class tab adds Schedule a Custom Class form for students to create their own group session; Platform presence tracking: POST /presence/ping every 60s from web (PresenceHeartbeat) and mobile, admin dashboard at /admin/presence shows live concurrent users, by platform, by activity, per-user last-seen.
- 2026-07-23 - v0.36.0 - Mobile: CorporateScreen and KidsScreen (Corporate Training and Kids Academy now accessible from HomeScreen action dock); Security: MFA brute-force lockout and password-reset single-use now Redis-backed for multi-pod correctness (falls back to in-process when Redis unavailable), _bounded_set_add evicts 50%% instead of clearing; Stem Cell Lab educational game (6 diseases: cancer, diabetes, Alzheimers, COVID-19, aging, skin/allergies); language_learning Khmer tts_text hardcode fixed.
- 2026-07-23 - v0.35.0 - Group Class page: clear 1-on-1 Private Lesson vs Group Class session type selector; two-tab layout (Teach a Class / Join a Class); auto-opens Teach tab from Live Class page teacher CTAs; Live Class page: two explicit teacher CTAs (private lesson + group class) instead of generic host link; nav: Group Class label (was Host a Class).
- 2026-07-23 - v0.34.0 - Salareen Worlds mobile port: virtual D-pad + action buttons (Jump/Interact/Attack) on mobile WebView; fullscreen button on desktop; Quit button (posts exit_game to native, or /arcade on web); 54 educational questions (was 22) across math/science/geography/history/biology/astronomy/language; AI opponent Theodore Bot that wanders world, answers questions, shows XP score.
- 2026-07-23 - v0.33.0 - Group Classes page: clearer host vs student distinction; Live room: screen share button, whiteboard moves inside host tile, presentation PiP camera (teacher face shrinks to corner when slides are active).
- 2026-07-23 - v0.32.0 - Fix 4 critical security bugs: SSRF via HTTP redirect bypass (add allow_redirects=False + DNS resolution check); OAuth email fallback account takeover (raise 409 instead of silently linking); payment confirmation grants access without verifying payment (check provider status before marking PAYMENT_PAID); assessment grade accepts client-supplied answer index (store quiz items server-side, remove answer_index from quiz response).
- 2026-07-23 - v0.30.0 - Home page: inline email/password sign-in form replaces the redirect to /login; users can sign in or create an account directly on the landing page; toggle between sign-in and sign-up; forgot password link preserved.
- 2026-07-23 - v0.29.10 - Fix CI: docker compose fails because config/local.env was removed from git (PR #395); add cp local.env.example local.env step to ci.yml compose job; remove obsolete version field from docker-compose.yml; change mobile pnpm install to --no-frozen-lockfile so CI does not break when lockfile lags package.json.
- 2026-07-22 - v0.29.9 - Fix CI lockfile mismatch: pnpm-lock.yaml missing react-native-webview@13.8.6 causing mobile-tests to fail on main; fix WorldsScreen onError/onHttpError implicit-any with inline types; fix stale test assertions for kids maturity filter and lesson deep_link.
- 2026-07-22 - v0.29.7 - Security: remove Facebook App Secret, admin/QA passwords from VCS; untrack config/cloud.env and config/local.env; fix .gitignore to block future commits; add config/local.env.example template.
- 2026-07-22 - v0.29.6 - Fix 19 Salareen Worlds game bugs: shockwave array never drained (now returns bool from filter), GPU leaks in combat/planets/crafting/WorldGame, NPC tara+elder keys missing from NPC_DIALOGUES, vehicle heightmap X/Z swap, PointLight leak on planet switch, clouds orbiting as moons, crafting pickups duplicating on planet switch, crafting inventory desync, WebGL try/catch, planet switch unhandled rejection, mobile WorldsScreen iOS back button + HTTP error handler + duplicate loading indicator, showWorlds missing from mainTabsVisible/sign-out/tab-change.
- 2026-07-22 - v0.29.4 - Fix Salareen Worlds game systems: repair 15 broken API call sites in WorldGame.tsx — VehicleManager (checkNearby/activeVehicle/enterVehicle/exitVehicle/fireLaser/update arg order), BuildingSystem (isBuilding property/toggle method).
- 2026-07-22 - v0.29.1 - Salareen Worlds mobile integration: embed Three.js web game in mobile app via react-native-webview 13.8.6 with XP postMessage bridge; Worlds button added to HomeScreen action dock; WorldsScreen with loading tips, error state, and XP toast.
- 2026-07-20 - v0.27.5 - Fix 8 voice-loop bugs: preserve awaitingConfirmation through SSE done event; decouple hasFloor cleanup from confirmation mic; clear awaitingConfirmation after finish_turn resolves; server-side 30s watchdog releases floor on client disconnect; clear stale trigger-phrase timer on slide change; read isFloorHolder from live ref at TTS-end time; rate-limit Theodore chat replies to 1 per 8s per participant; skip chat-reply TTS during active confirmation window. while the
  bot appended empty "[0.2.x] - release build (no itemized changes)" sections.
  The regex now matches both forms; the corrupted cascade was collapsed and the
  accumulated notes consolidated into [0.3.0].
- training: add offline (Linux/Ubuntu) training code. training/export.py builds
  JSONL from class sessions/feedback; training/run_finetune.py runs QLoRA/LoRA
  fine-tuning (transformers+peft+trl, 4-bit on CUDA, CPU/LoRA fallback, fully
  offline via HF_HUB_OFFLINE) with a torch-free --check dry run;
  training/evaluate.py scores a checkpoint; training/train.sh is a one-command
  Ubuntu runner. Fairness guardrail (no protected attributes) still enforced.

[0.3.0] - 2026-06-17
- tutor: culture-aware slang & idiom understanding (aoep_shared.slang). A
  data-driven lexicon keyed by language + region (US/UK English, Mexican Spanish,
  French, Khmer, ...) with a normalizer that detects idioms/slang and expands
  them to plain meaning (longest-match, no overlap). The Tutor normalizes a
  question before RAG/LLM so "it's a piece of cake" is understood as "very easy",
  surfaces recognized expressions in the answer (understood[]), and a new
  POST /api/slang/normalize endpoint exposes it.
- perception: machine-vision engagement + cross-session student memory.
  Cross-session memory: FaceGallery persists embeddings (save_json/load_json);
  VisionProvider loads/saves at VISION_GALLERY_PATH so enrolled students are
  remembered across sessions/restarts. Engagement (aoep_shared.vision.engagement):
  real gaze/attention + a coarse expression proxy (neutral/smiling/surprised)
  from the detector's 5 landmarks; analyze_image returns attention/gaze_frontal/
  expression. GestureRecognizer interface scaffolded. Engagement feeds the
  phase-4 adaptive policy.
- web: audience-tailored landing templates. Data-driven theme registry
  (apps/web/app/lib/themes.ts) + a Template switcher: Corporate, Khmer Language,
  Culinary, Kids & Gamified, Academic. Selection persists and is
  shareable via ?theme=. Themed art under apps/web/public/themes/.
- format: AOEPLX layered live-teaching scene format (aoep_shared.scene +
  docs/aoeplx-format.txt). Ordered layers (video/audio/image/whiteboard/text/
  note/quiz/poll) with transforms, time ranges, anchored attachments; live
  SceneDeltas; extract_region lifts a bbox/time slice into a multilayer object
  (image/clip); tamper-evident SHA-256 + HMAC container. Curriculum exposes
  /scenes (+delta/extract/sign) and /scenes/verify.
- scraper: content ingestion -> presentation decks (curriculum.ingest). Extracts
  from textbook PDFs (pypdf), web/HTML (BeautifulSoup), and video/YouTube
  transcripts; generates genre-tagged decks (lecture/hands_on/tutorial/video/
  article) stored in the CMS. Endpoints POST /ingest/pdf,/html,/url,/transcript,
  /youtube.
- training: scaffold the education-LLM training subsystem (training/). Tested
  data pipeline (pipeline.dataset/feedback) turning class transcripts + audience
  context + feedback into reward-weighted JSONL; finetune config. FAIRNESS
  GUARDRAIL: protected attributes (race, ethnicity) excluded from conditioning.
- version: single source-of-truth version (aoep_shared.version.get_version) in
  every service /health + OpenAPI and the web nav; Dockerfiles COPY VERSION;
  build_release syncs apps/web version.
- ci: Release workflow (.github/workflows/release.yml) cuts a GitHub Release
  (tag vX.Y.Z + CHANGELOG notes) when VERSION advances; include training/tests.
- docs: README build-status table updated to reflect implemented phases.
- phase6: curriculum CMS - deck authoring/import/management (SlideSpec/Deck +
  DeckStore + plain-text import) with /decks endpoints.
- phase2: multilingual delivery routing (aoep_shared.translation) across 26
  languages; speech /delivery/plan + /translate.
- phases7-9: media bridge scaffold (aoep_shared.bridges) - MediaBridge interface
  + Zoom/Teams(.NET)/Meet registry + credential-readiness checks.
- phase10: latency budget + recorder (aoep_shared.observability).
- refactor: consolidate to a single stack (remove duplicate eduplatform_shared +
  services/*/app/ layouts; rename dist to aoep-shared; port the web teaching loop
  into the aoep orchestrator with an offline grounded fallback).
- phase4: adaptive learning policy (aoep_shared.adaptive) -> pacing/difficulty/
  re-teach; /director/plan; memory /behavior + /learner.
- phase5: assessment (aoep_shared.assessment) - MCQ generation, difficulty-
  weighted grading, polls; /assessment/quiz + /assessment/grade.
- billing: multiple payment methods (card, Apple/Google Pay, Cash App, PayPal,
  Venmo, Zelle) behind PaymentProvider; /payment-methods + method-aware /checkout.
- perception: real self-hosted face recognition (OpenCV YuNet + SFace, CPU),
  consent-gated enroll/identify; /enroll, /identify, /gallery.

[0.2.2] - 2026-06-17
- ci: add auto-merge workflow (.github/workflows/automerge.yml) that merges PRs
  into main automatically after the CI workflow passes (no human review).
- docs: add comprehensive README.md (overview, screenshots, per-phase build
  status, architecture, quickstart, run/test instructions, dual-mode config) with
  brand/design imagery and real app screenshots under docs/images/.

[0.2.0] - 2026-06-17
- phase0: implement foundations end to end (monorepo, dual-mode, CI).
- packages/shared: AppConfig dual-mode loader (DEPLOY_MODE + per-component
  *_MODE overrides), provider interfaces (LLM/Speech/Vision/Media/ObjectStore/
  Payment) with local+cloud impls, config-driven ProviderFactory, cross-service
  pydantic schemas, 26-language coverage + TTS fallback policy, entitlements
  can_start gate, dependency-free RAG skeleton, shared FastAPI /health + CORS.
- services: orchestrator (Teaching Director state machine + class/join/token),
  speech (languages + TTS engine routing), perception (consent-gated identity),
  memory (profiles/consent/mastery), curriculum (RAG search), billing
  (plans/entitlements/checkout). Each exposes /health and has a Dockerfile.
- apps/agent-runtime: provider-driven TeachingBrain + LiveKit worker entrypoint.
- apps/web: Next.js (app router) dashboard, live-class join, consent UI.
- config/local.env + config/cloud.env (single dual-mode contract).
- db/migrations/0001_init.sql: identity, classes, consent/compliance (FERPA/
  GDPR/BIPA), face embeddings, mastery, curriculum vectors, billing tables
  (pgvector + pgcrypto).
- infra/compose: full local stack; infra/k8s: namespace/config/services/web +
  GPU vLLM serving; models/serving + models/finetune configs.
- bridges/{zoom,teams,meet}: phase7-9 scaffolds.
- CI: GitHub Actions (python tests, web typecheck/build, compose + k8s render);
  Makefile.
- verified: 49 Python tests pass; web typechecks + builds; live local-mode
  endpoints and the local/cloud provider switch exercised over HTTP.

[seed] - 2026-06-16
- seed: initialize repository for Cursor Cloud Agent execution.
- add docs/plan.txt (architecture + phased roadmap, plain text).
- add docs/cloud-agent-task.txt (phase-by-phase implementation brief).
- add .gitignore, .editorconfig, .python-version.

Note: versions 0.2.1 and 0.2.3-0.2.14 were automated build bumps whose notes
were lost to the changelog-rolling bug fixed in the release workflow; their content is
archived in git history only.

[0.3.83] - 2026-06-24
- (no changes yet)

[0.3.82] - 2026-06-23
- (no changes yet)

[0.3.81] - 2026-06-23
- (no changes yet)

[0.3.80] - 2026-06-23
- (no changes yet)

[0.3.79] - 2026-06-23
- (no changes yet)

[0.3.78] - 2026-06-23
- (no changes yet)

[0.3.77] - 2026-06-23
- (no changes yet)

[0.3.76] - 2026-06-23
- (no changes yet)

[0.3.75] - 2026-06-19
- (no changes yet)

[0.3.74] - 2026-06-19
- (no changes yet)

[0.3.73] - 2026-06-19
- (no changes yet)

[0.3.72] - 2026-06-19
- (no changes yet)

[0.3.71] - 2026-06-19
- (no changes yet)

[0.3.70] - 2026-06-19
- (no changes yet)

[0.3.69] - 2026-06-19
- (no changes yet)

[0.3.68] - 2026-06-19
- (no changes yet)

[0.3.67] - 2026-06-19
- (no changes yet)

[0.3.66] - 2026-06-19
- (no changes yet)

[0.3.65] - 2026-06-19
- (no changes yet)

[0.3.64] - 2026-06-19
- (no changes yet)

[0.3.63] - 2026-06-19
- (no changes yet)

[0.3.62] - 2026-06-19
- (no changes yet)

[0.3.61] - 2026-06-19
- (no changes yet)

[0.3.60] - 2026-06-19
- (no changes yet)

[0.3.59] - 2026-06-19
- Netflix-compatible content interchange: Course gains streaming/interchange
  metadata (maturity_rating, subtitle_languages, hls_url, dash_url, trailer_url);
  curriculum/interchange.py + GET /catalog/export?format=json|mrss produce an
  acquisition-ready content feed (JSON) and a Media RSS (MRSS) XML feed with
  media:content/thumbnail/rating + HLS/DASH manifest refs - mappable to streaming-
  partner onboarding specs. Tested.
- Video-ad monetization: aoep_shared/ads.py builds IAB VAST/VMAP ad schedules
  (pre-roll + capped mid-rolls at cue points), tier-gated (free/basic see ads,
  pro/premium ad-free). GET /courses/{id}/ad-breaks?tier=&format=json|vmap +
  web /watch player demo (skippable ads, mid-roll cues, ad-free upgrade). Tested.

[0.3.58] - 2026-06-19
- release build (no itemized changes)
