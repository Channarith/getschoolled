CHANGELOG - Agentic Online Education Platform
=============================================
Format: newest first. No markdown per project convention.

[unreleased] - 2026-06-17
- ci: add auto-merge workflow (.github/workflows/automerge.yml) that merges PRs
  into main automatically after the CI workflow passes (no human review).
- phase0/phase1: scaffold runnable dev environment for the platform.
- packages/shared: provider abstraction (LLM/Speech/Vision/Media/ObjectStore/
  Payment) with local + cloud implementations and a config-driven factory;
  dual-mode Settings (DEPLOY_MODE + per-component overrides; blank override
  resolves to inherit); shared schemas; service health helper; factory tests.
- services/orchestrator: FastAPI Teaching Director - lessons, sessions, slide
  delivery, and RAG-grounded Q&A via the LLM provider; API tests.
- services/{memory,speech,perception,curriculum,billing}: FastAPI skeletons with
  /health plus a domain stub each (consent-gated attention, entitlements, etc.).
- apps/agent-runtime: importable LiveKit Agents worker skeleton + tests.
- apps/web: Next.js live-class UI (home, live class, dashboard, consent) that
  drives the orchestrator teaching loop; lint/typecheck/build clean.
- config/local.env + config/cloud.env; db/migrations (consent/compliance +
  billing/entitlements); models/serving/vllm.yaml; sample-curriculum for RAG.
- infra/compose docker-compose (web, services, LiveKit, Postgres+pgvector,
  Redis, MinIO); per-service Dockerfiles; root Makefile; GitHub Actions CI.

[seed] - 2026-06-16
- seed: initialize repository for Cursor Cloud Agent execution.
- add docs/plan.txt (architecture + phased roadmap, plain text).
- add docs/cloud-agent-task.txt (phase-by-phase implementation brief for the agent).
- add .gitignore, .editorconfig, .python-version.
